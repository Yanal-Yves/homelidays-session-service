TODO:
	ASP:
		- Lors du premier appel à la session les tables sont créer et la page lève une exception.

	Common:
		- Renomer la balise <Dictionary> en <Object> qui est utilisé pour Dictionary et pour null (et tout autre futur composant COM)
		- Partition resolver does not allow multiple partition but only one partition.

	CPP:
		- Intégration Homelidays : trouver une solution pour palier au Session_OnStart
		  => pour l'instant j'ai créé la directive de préprocesseur HOMELIDAYS_SESSION_ONSTART qui compile du code spécific Homelidays car je n'ai pas trouvé comment faire autrement.

	SQL Server:
		- Optimisation de la base:
			- Utiliser un smalldatetime pour le lastaccessed au lieu d'un datetime.

	C#:
		- Gestion de Dictioanry en C# pour ne pas le perdre entre asp.net et asp.

	Setup:
		- Setup should allow users to edit the connection string
		- The cleaning SQL job should install automaticaly
		- Ease the "Setup" build that is too complicated

-----------------------------------------------------------------------------------------------------------------------

How to generate the setup:
----------------------
- Se mettre en Release
- Se mettre en Win32 ou x64
- Faire F4 sur le projet Setup et régler TargetPlatform
- Faire bouton Droit propriété sur Setup et régler le runtime C++ (Win32 ou x64)